# speciai-kakao-bot — Claude 가이드

## 정체

거래처 카카오톡 단톡방을 개인 카톡과 분리해 모아 보는 사내 콘솔. 단독 실행형 Next.js 앱.
`kakao-advisor-bot`(노무사무소용 자문방 수집)의 콘솔 셸·인증·CSS 를 가져오고,
카톡 연동 도메인은 전부 새로 짰다.

**범위 밖**: 자문 답변 초안, 이슈 자동분류, 법령 조문, FAQ 자동응답, 상담톡 채널 webhook,
대화 내보내기 .txt 업로드. advisor 에서 의도적으로 뺀 것들이니 되가져오지 말 것.

## 절대 원칙

1. **거르는 곳은 서버다** (`DEVICE_FILTER=false`, 2026-08-12 대표 지시로 전환).
   봇 단말은 이 폰에 오는 카톡을 전부 올리고, 서버가 규칙에 걸리는 방만 저장한다.
   규칙 밖 방은 **본문을 저장하지 않는다** — `api/kakao/bot/ingest` 가 사진 업로드·메시지
   저장보다 **먼저** 매칭을 보고, 미매칭이면 `kakao_unmatched_rooms` 에 방 이름·수신 횟수만
   남기고 버린다. 이 "매칭 먼저" 순서를 뒤집지 말 것 — 뒤집는 순간 개인 대화와 사진이
   DB·버킷에 남는다.
   감수한 대가: 개인 대화가 전송 구간을 지나가고, 미분류 방 목록에 개인 카톡방 이름
   (=사람 이름)이 쌓인다. "왜 개인 방이 목록에 뜨냐" 로 되돌리지 말 것.
   사진만은 단말에서 먼저 거른다(`PHOTO_ONLY_FROM_KNOWN_ROOMS`) — 장당 3MB 를 올려봐야
   서버가 버리므로 비용만 든다.
   되돌리려면 봇의 `DEVICE_FILTER = true` 한 줄이면 된다.
2. **fail-closed.** 규칙을 못 받았으면 봇은 아무것도 보내지 않는다. 전부 보내는 쪽으로 실패시키지 말 것.
3. **AI 추론 시스템 아님** — 매칭·멱등·집계는 전부 결정론. Anthropic 의존성이 없다.
4. **봇이 방에 쓰는 것은 사람이 대시보드에서 쓴 글뿐이다.** 자동응답·자동인사·자동확인을
   넣지 말 것 — 기계적 응답 패턴은 카카오의 자동화 탐지를 부르고, 봇 계정이 정지되면
   **수집까지 함께 멈춘다.** 발신 간격(4초+무작위 지연)도 같은 이유이니 줄이지 말 것.
5. **워크스페이스는 하나다. 그 하나를 정하는 것은 `KAKAO_WORKSPACE_ID` 다**
   (2026-08-14 대표 지시). 봇도 콘솔도 이 값이 가리키는 워크스페이스만 본다
   (`resolveBotWorkspace` · `src/lib/auth/server.ts`). **배포에서 이 값을 비우지 말 것** —
   비우면 워크스페이스가 둘 이상 되는 순간 봇이 멈추고, 콘솔은 사람마다 다른 곳을 본다.
   새 계정은 워크스페이스를 **만들지 못한다**(`createWorkspace` 가 거부하고 가입 직후 화면은
   승인 대기만 보여준다). 들어오는 길은 관리자 승인 하나뿐이다
   (멤버 관리 → 가입 대기 → `grantAccess`). 이 경로를 늘리지 말 것 — 만들기 버튼 하나가
   사내 데이터를 둘로 쪼갠 적이 있다(2026-08-13, 아래 참조).

## 스택

- Next.js 15 App Router · React 19 · TypeScript strict
- Supabase (Postgres + Auth + Storage), RLS 워크스페이스 격리
- 모노레포 아님. 경로 별칭 `@/*` → `src/*`

## 방 → 거래처 연결 (핵심)

**대시보드는 회사명만 관리한다. 방 연결은 카톡방 안에서 명령으로 만든다.**

```
#등록 삼성전자    이 방을 그 거래처에 붙인다 (거래처가 이미 있어야 한다)
#등록해제         이 방의 수집을 멈춘다 (모아둔 대화는 남는다)
```

방 이름 패턴을 사람이 손으로 맞추게 하지 않는다. 대괄호 하나 빠진 규칙이 섞이면 그 방은
아무 소리 없이 수집되지 않고, 몇 주 뒤에야 발견된다. 방에서 한 번 선언하는 쪽이 틀릴 여지가 적다.

| 테이블 | 역할 |
|---|---|
| `partners` | 거래처 — **회사명만**. 대시보드에서 만든다 |
| `partner_room_rules` | 방 연결. `#등록` 은 `kind='exact'`, `pattern=방 이름` 1건을 만든다 |
| `kakao_rooms` | 실제 카톡 방. 연결된 방이면 자동 생성 |
| `kakao_messages` | 메시지 |
| `kakao_unmatched_rooms` | 연결 안 된 방 — **이름과 횟수만**, 본문 저장 안 함 |
| `kakao_outbound` | 대시보드 → 카톡방 발신 큐 |

바인딩 전용 테이블을 두지 않고 `exact` 규칙으로 표현하는 이유: 규칙 조회·봇 배포·콘솔 편집이
전부 기존 경로를 그대로 탄다. 매칭 경로가 둘로 갈라지지 않는다.

`prefix`/`contains`/`regex` 는 **새로 만들지 않는다**. 패턴 방식이던 시절의 행이 남아 있을 수
있어 엔진과 enum 은 그대로 두고, 콘솔이 "예전 패턴 규칙" 으로 보여주고 지울 수 있게 한다.
매칭 우선순위(`src/server/kakao/rules.ts`): `priority` 큰 것 → 패턴 긴 것 → 사전순.
`#등록` 은 `priority: 100` 이라 남아 있는 접두어 규칙보다 먼저 걸린다.

### 없는 회사명으로 #등록 하면 거부한다

방에서 친 오타로 "삼성전쟈" 같은 거래처가 생기면 그 방 대화가 조용히 엉뚱한 곳에 쌓인다.
거래처를 만드는 곳은 대시보드 한 곳뿐이다. 거부된 방은 `kakao_unmatched_rooms` 에 남아
콘솔 "연결 안 된 방" 목록에 뜬다.

### ⚠️ 같은 판정이 두 곳에 있다

| 서버 | 봇 단말 |
|---|---|
| `src/server/kakao/rules.ts` 의 `ruleMatches` | `bot/speciai-bot.js` 의 `ruleMatches` |
| `src/server/kakao/commands.ts` 의 `parseRoomCommand` | `bot/speciai-bot.js` 의 `isRoomCommand` |

어긋나면 "단말은 보냈는데 서버가 버리는" 방이 생기거나, 단말이 통과시킨 명령을 서버가
일반 메시지로 저장해버린다. 문법을 바꾸면 반드시 양쪽을 함께 고치고
`rules.test.ts`·`commands.test.ts` 를 갱신할 것.

## 수집 경로

경로는 하나뿐이다 — 온디바이스 봇(메신저봇R).

| 진입점 | 인증 | 용도 |
|---|---|---|
| `POST api/kakao/bot/ingest` | `X-Ingest-Token` | 메시지 1건 인입 |
| `GET api/kakao/bot/rules` | `X-Ingest-Token` | 봇이 받아갈 방 필터 규칙 |
| `POST api/kakao/bot/outbox` | `X-Ingest-Token` | 발신 결과 보고 + 보낼 것 수령 |

셋 다 세션이 없어 service-role 클라이언트로 RLS 를 우회한다.
**봇 라우트를 새로 만들면 `src/lib/auth/middleware.ts` 의 `PUBLIC_PATHS` 에 반드시 추가할 것** —
빠뜨리면 미들웨어가 로그인 화면으로 307 리다이렉트하고, 봇은 HTML 을 받아 조용히 실패한다.
워크스페이스는 `KAKAO_WORKSPACE_ID` 로 지정한다(`resolveBotWorkspace`).
미설정이면 워크스페이스가 정확히 하나일 때만 폴백하고, 둘 이상이면 **인입을 거부한다** —
엉뚱한 곳에 거래처 대화를 쌓는 것보다 안 받는 편이 낫다.

### ⚠️ env 의 워크스페이스 ID 는 실재 여부를 확인한다

값이 있으면 그냥 쓰던 시절, 배포 환경이 바뀌며 **지워진 워크스페이스의 UUID** 가 남았다.
봇은 `POST 200` 을 받고, 서버는 그 유령 워크스페이스에 계속 쌓고, 화면은 계속 비어 있었다.
`#등록` 은 `rejected: partner-missing` 으로 돌아왔다 — 거래처를 없는 워크스페이스에서
찾았으니까. 원인을 하루 종일 봇 코드에서 찾았다(2026-08-12).

그래서 `resolveBotWorkspace` 는 env 값이 `workspaces` 에 실재할 때만 쓰고, 없으면
**503 + 사유**를 돌려준다(사유가 봇 로그에 그대로 찍힌다). **실재하지 않아도 폴백하지 않는다** —
명시된 설정을 서버가 말없이 다른 곳으로 바꾸는 것도 "엉뚱한 곳에 쌓기" 다.

교훈: **조용히 성공하는 실패가 제일 비싸다.** 인입 경로에서 "받았는데 어디론가 갔다" 가
가능한 분기를 만들지 말 것.

### ⚠️ 화면이 자기 워크스페이스 ID 를 정답으로 내밀지 말 것 (2026-08-13 사고)

한동안 봇 연동 탭이 "환경변수가 다른 워크스페이스를 가리킵니다 — 이 값으로 고치세요" 라며
**보고 있는 사람의 워크스페이스 ID** 를 복사용 입력칸에 띄웠다. 잘못된 워크스페이스에 들어간
사람일수록 그 안내를 확신을 갖고 따른다. 실제로 대표 계정이 그 값을 Vercel 에 넣었고,
그때부터 봇 수집이 통째로 빈 워크스페이스로 넘어갔다. 봇 로그는 `POST 200`, 규칙 배포는
`rules:[]`, 모든 방이 "연결 안 된 방" — 서버는 아무 잘못이 없어 보였다.

그래서 방향을 뒤집었다. **환경변수가 앵커고 세션이 거기에 맞춘다**(`src/lib/auth/server.ts`).
`KAKAO_WORKSPACE_ID` 가 설정돼 있으면 콘솔은 그 워크스페이스만 보고, 그 워크스페이스의
멤버가 아닌 계정에는 워크스페이스가 **없는 것으로 본다**(→ 승인 대기 화면).
"불일치" 라는 상태가 존재할 수 없으므로 그 배너도 없앴다. 남은 경고는 값이 **비었을 때**뿐이다.

`workspaceIdConfigured` 를 다시 "세션과 같은지" 로 되돌리지 말 것 — 세션이 env 를 따라가므로
항상 참이고, 거짓이 되는 유일한 길은 위 사고를 되살리는 것이다.

## 대시보드 발신 (`kakao_outbound`)

**서버는 카톡에 직접 말할 수 없다.** 알림에 실린 RemoteInput 세션을 가진 것은 봇 단말뿐이다.
그래서 대시보드의 "보내기" 는 전송이 아니라 **큐 적재**다. 화면은 이 사실을 숨기지 말 것 —
`pending`(대기) · `sent`(나감) · `failed`(3회 실패)를 그대로 보여준다.
업무 카톡에서 제일 나쁜 건 "보낸 줄 알았는데 안 갔다" 이다.

```
대시보드 입력 → POST api/kakao/send → kakao_outbound(pending)
  → 봇이 가져감(claim, status=sending) → 방에 전송 → 결과 보고(ack)
  → sent 면 kakao_messages 에 side='us' 로 복사 (content_hash = out:<id>)
```

봇이 가져가는 경로는 둘이다. **인입 응답에 얹혀 가는 것**(거래처가 방금 말한 방 — 세션이
가장 확실한 순간)과 **15초 폴링**. 둘 다 같은 claim 을 타므로 중복 전송이 되지 않는다.
`sending` 인 채 2분이 지나면 폰이 죽은 것으로 보고 `pending` 으로 되살린다.

- 카톡에 나가는 문자열은 `[담당자] 본문`(`composeWireText`). 봇 계정 하나로 여러 담당자가
  답하므로 접두가 없으면 거래처는 누가 말하는지 알 수 없다. 이 이름은 `profiles.display_name`
  이고 설정 탭 "내 발신 이름" 에서 담당자별로 바꾼다 — 가입 시 이메일 앞부분으로 자동
  설정되므로 두면 아이디(`martin1023`)가 거래처 방에 그대로 찍힌다.
- **봇에게 내려보내는 방 이름은 규칙 `pattern` 이지 화면에 보이는 이름이 아니다.**
  봇은 받은 이름을 자기 규칙과 대조해 "규칙에 없는 방에는 안 쓴다" 를 한 번 더 검사한다.
  `room_name` 은 거래처명으로 갈아 끼워져 있어 규칙에 안 걸린다 — 그걸 내려보냈다가
  발신이 전부 단말에서 거부됐다(2026-08-12). `claimOutbound` 가 `matched_rule_id` 로
  패턴을 읽어 보낸다. 표시 이름을 바꿀 때는 이 경로를 같이 볼 것.
- 보낸 메시지는 **수집 경로로 돌아오지 않는다** — 봇 폰이 스스로 보낸 것은 그 폰에 알림이
  뜨지 않는다. 그래서 ack 시점에 서버가 `kakao_messages` 에 직접 남긴다. 이걸 지우면
  대화 기록에서 우리가 한 말만 빠진다.
- 봇은 **규칙에 없는 방으로는 보내지 않는다**(단말에서 한 번 더 검사). 서버가 잘못된 방
  이름을 내려보내도 개인 카톡방에 글이 써지지 않는다.
- 서버가 내려보내는 방 이름은 **우리가 교정한 이름**이라 `Api.replyRoom(그 이름)` 이 통하지
  않는다 — 메신저봇R 은 그 방을 발신자명으로 알고 있다. 그래서 `sendToRoom` 은
  ① `bot.send`(API2) ② 그 방에서 마지막으로 받은 `replier` ③ **알림의 RemoteInput**
  ④ `Api.replyRoom`(메신저봇R 이 아는 원래 이름 → 교정된 이름) 순으로 시도한다.
  API2 가 없는 단말에서 실제로 나가는 것은 ②·③ 이다. 셋 다 알림 세션을 물고 있어
  방이 오래 조용하면 만료되고, 그때는 `failed` 로 대시보드에 뜬다.
- viewer 는 발신 불가. RLS 는 멤버 여부만 보므로 판정은 `api/kakao/send` 에서 role 로 한다.

## 멱등 규칙

`content_hash` = 봇이 `logId`(API2 메시지 id)를 주면 `log:<logId>`, 없으면
`md5(분단위시각|발화자|본문)`. 유니크는 `(room_id, content_hash)`.

advisor 와 달리 **부분 인덱스가 아니라 완전 유니크**라 supabase-js
`upsert(onConflict:'room_id,content_hash', ignoreDuplicates:true)` 가 정상 동작한다.
분 단위로 자른 이유: 봇 재전송·API2/구API 동시 수신 같은 중복은 초 단위로 생기므로 잡히고,
나중에 같은 사람이 같은 말("네")을 다시 해도 별개로 남는다.

## 방 식별

`room_key` = `chat:<chatId>` (API2 가 chatId 를 줄 때) 또는 `name:<정규화된 방이름>`.
chatId 가 있으면 방 제목을 바꿔도 같은 방으로 이어진다.

실측 주의: 메신저봇R 은 NotificationListenerService 기반이라 단말에 따라 `chatId`·`logId` 가
아예 없고 `chat.room` 이 문자열인 경우가 있다. 그 경우 방 이름이 유일한 단서다.
게다가 방 제목 대신 **발신자명**이 오는 단말이 있어, 봇이 알림 원본에서 방 제목을 복원한다.
이 복원을 지우면 규칙이 걸리지 않아 수집이 통째로 멈춘다.

### ⚠️ 이 단말의 카톡 알림에는 방 제목이 안 보인다 (실측 2026-08-12)

알림에 뜨는 것은 **(발신자 + 내용)** 이다. 단톡방·오픈채팅·1:1 전부 그렇다.

| extras 키 | 이 단말의 실측값 |
|---|---|
| `android.title` | **발신자명** ("신동규") — 방 제목이 아니다 |
| `android.conversationTitle` | 빈 값 |
| `android.subText` | 빈 값 |
| `android.hiddenConversationTitle` | **방 제목이 여기 숨어 있다** |
| `android.messages[].sender_person` | 발신자 |
| `android.isGroupConversation` | true |
| `threadId` · `chatLogId` | 카톡 자체 식별자 |

`android.title` 을 방 제목으로 읽으면 안 된다. 발신자명이다.
방 제목은 `ROOM_TITLE_KEYS`(conversationTitle → **hiddenConversationTitle** → subText →
summaryText → infoText) 순으로 찾는다. 카톡이 화면에 안 띄우는 제목을 보관하는 표준 자리가
`hiddenConversationTitle` 이고, 이 단말에서는 거기가 유일한 통로다.
`android.isGroupConversation` 은 `java.lang.Boolean` 이라 `!!` 로 읽으면 false 도 true 가 된다.

어느 칸에도 제목이 없으면 알림 신원(`threadId` → `chatId` → `tag` → `id` → `sbn.getKey()`)으로
`방#<열쇠>` 라는 이름을 지어 붙인다. 실측값은 `방#476647316580151` — 카톡이 실어 보내는
진짜 채팅방 ID 다. 이름은 못 얻어도 방 구분은 되므로 `#등록` 은 성립한다.

**그 열쇠는 매칭에만 쓰고, 화면에는 거래처명을 보여준다.** `ingestBotMessage` 가
`isGeneratedRoomName(roomName)` 이면 `kakao_rooms.room_name` 을 거래처명으로 갈아 끼운다.
`room_key` 와 규칙 `pattern` 은 그대로 열쇠다 — 표시만 바꾸는 것이니 매칭 경로를 건드리지 말 것.

**1:1 개인톡은 미분류 목록에 올리지 않는다.** 1:1 방 이름은 상대 이름이라(`room === speaker`)
목록이 개인 연락처로 채워지고 정작 찾아야 할 거래처 방이 묻힌다. 봇 단말에서도 등록 안 된
1:1 은 아예 보내지 않는다(선필터를 꺼둔 상태에서도). 등록된 1:1 방은 그대로 수집된다.
방 등록은 방 안에서 `#등록 <회사명>` 으로만 하므로 목록에 없어도 지장이 없다.

**1:1 방에도 이름이 있다** — 상대 이름(`android.title`)이 곧 방 이름이다. 이걸 빈 값으로
두면 "제목을 못 읽은 단톡방" 과 구별이 안 되고, 빈 칸을 직전 방으로 메우려는 순간 개인
대화가 거래처 방으로 새어 들어간다(2026-08-11 사고). 어느 칸도 비우지 않는 것이 유출을
막는 방법이지, 물려받기 플래그를 켜고 끄는 게 아니다.

`chatLogId` 를 멱등키로 쓰지 말 것 — 값이 방 단위로 고정된 것이라면 그 방의 두 번째
메시지부터 전부 "중복" 으로 조용히 버려진다. 로그로 방마다·메시지마다 다른지 확인한 뒤에.

### 수집 트리거가 둘이다 — 순서를 가정하지 말 것

알림 훅(`handleKakaoNoti`)과 메신저봇R 의 메시지 콜백(`response`/`Event.MESSAGE`)은 서로
독립된 이벤트다. `response` 가 먼저 오면 그 시점에 알림 기록이 없어 방 제목을 못 붙인다.
그래서 알림 훅도 곧바로 수집한다(`NOTI_INGEST`). 중복은 단말의 `_claimMessage`(20초 창)가
먼저 막고, 새더라도 서버 멱등키 `(room_id, content_hash)` 가 한 번 더 막는다.
알림 콜백은 메인 스레드로 오는 단말이 있어 인입은 반드시 별도 스레드에서 돈다.

## 첨부 (사진·파일)

**둘 다 같은 통로로 온다.** 안드로이드 MessagingStyle 알림의 각 메시지 번들에 `uri`(첨부의
`content://` 주소)와 `type`(MIME)이 실린다. `NotificationListenerService` 는 알림에 실린 URI 에
일시적 읽기 권한을 받으므로 카톡의 첨부를 그대로 열 수 있다 — **종류를 가리지 않는 권한이라
사진이 되면 PDF 도 된다.**

한동안 파일이 안 됐던 이유는 권한이나 API 가 아니라, 받은 스트림을 무조건
`BitmapFactory.decodeStream` 에 넣고 null 이면 "사진 실패" 로 버렸기 때문이다. 지금은
`type` 을 먼저 보고 갈래를 나눈다(`extractNotiAttachment`).

| | 사진 | 파일(PDF·DOCX…) |
|---|---|---|
| 봇 → 서버 | `image`·`imageName` | `file`·`fileName`·`fileMime` |
| 크기 처리 | 1600px·JPEG80 으로 줄여 보냄 | **줄일 수 없다** — base64 3MB 넘으면 이름만 |
| 저장 | `kakao-attachments` 버킷, `attachment.type='image'` | 같은 버킷, `type='file'` |
| 화면 | 말풍선 안에 미리보기 | `📎 이름` 링크 |

- **`fileName` 만 오고 `file` 이 없으면 "이름은 알지만 바이트는 못 실었다"** 는 뜻이다
  (상한 초과·재전송 큐를 거친 경우). 서버는 본문에 `[파일] 이름` 을 적는다. 이걸 지우지 말 것 —
  무엇이 왔는지조차 안 남으면 사람이 카톡에서 그 파일을 찾을 단서가 없다.
- **URI 권한은 알림이 살아 있는 동안만 유효하다.** 도착 즉시 바이트까지 다 읽어야 하고,
  큐에 넣었다가 나중에 읽는 구조로 바꾸면 조용히 실패한다.
- 첨부는 **규칙에 걸리는 방일 때만** 연다(`PHOTO_ONLY_FROM_KNOWN_ROOMS`). 서버가 어차피
  버리기도 하지만, 그보다 개인 카톡의 사진·문서가 단말 메모리를 거치게 할 이유가 없다.

## 발화자 판정

`workspaces.staff_aliases`(설정 탭에서 등록)에 있는 이름이면 `side='us'`, 아니면 `'partner'`.
부분일치는 3자 이상 별칭에만 적용한다 — "김" 같은 짧은 별칭이 거래처 "김부장" 을 오판정한다.

## 브랜딩

회사 고유 문자열은 전부 `src/lib/brand.ts` + `NEXT_PUBLIC_BRAND_*` 로 뺐다.
새 하드코딩을 넣지 말 것.

## ⚠️ Supabase 프로젝트를 linktalk 과 함께 쓴다

같은 DB 에 앱이 둘 있다. 테이블 이름이 겹치지 않아 평소엔 안 보이지만, **소속 판정만은 공유**한다.

| 앱 | 소속 테이블 | 테이블 |
|---|---|---|
| linktalk | `workspace_members` (역할 없음) | threads, messages, customers, subscriptions … |
| 이 앱 | `memberships` (owner/admin/viewer) | kakao_*, partners, partner_room_rules, invitations |

`workspaces` · `profiles` · RLS 헬퍼 `is_workspace_member()` 는 둘이 같이 쓴다. 그래서 이 함수는
**두 테이블의 합집합**을 본다(`0005_shared_membership.sql`). 한쪽만 보게 바꾸면 반대쪽 앱이
통째로 막힌다.

`0001_base.sql` 에는 이 함수가 `memberships` 만 보도록 적혀 있다 — **운영 DB 에 재실행하지 말 것.**
`migrate-all.local.sql` 도 같다(맨 앞에서 테이블을 DROP 한다).

증상 기억해둘 것: 권한을 줬는데 화면에 **"열람"** 으로 뜨고 카톡방이 0개면 역할이 `null` 이라는
뜻이고, 대개 RLS 가 `memberships` 행 자체를 가린 것이다. 역할 조회가 null 이면 화면은 '열람'
으로 표시되므로 권한 설정 실수처럼 보인다(2026-08-13).

## 개인정보

- 거래처 방 대화는 원문 그대로 저장한다(마스킹 없음). 접근 통제는 RLS 에 의존한다.
- 미분류 방은 **방 이름과 수신 횟수만** 남긴다. 본문·발화자는 저장하지 않는다.
- 규칙을 나중에 등록해도 미분류 구간의 지난 대화는 되살아나지 않는다(애초에 저장하지 않았다).
- 각 거래처 방 참여자에게 자동 수집·저장 사실을 사전 고지·동의받아야 한다(개인정보보호법 §15).

## 코딩 규칙

- 파일명·폴더명 kebab-case, Next.js 라우트 컨벤션 우선
- 코드 식별자는 영문. 한국어는 UI 문자열·주석에만
- 주석은 "왜"를 적는다. 코드가 말하는 "무엇"을 반복하지 말 것
- supabase-js 는 쿼리 실패를 throw 하지 않는다 — `error` 를 반드시 보고 로깅할 것
- `bot/speciai-bot.js` 는 Rhino(ES5) 환경이다. 화살표 함수·`let`·`const`·템플릿 리터럴 금지

## 검증

```bash
npm run typecheck && npm test && npm run build
```
